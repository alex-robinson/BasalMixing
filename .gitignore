.DS_Store
*Manifest.toml
plots
logs
*.jld2

# runme: per-host config (gitignored; scaffolded by `runme config init`) and
# run/output directories.
.runme/config.toml
output/

# LaTeX build artefacts (the .pdf and .tex are tracked; everything else is regenerable)
docs/*.aux
docs/*.log
docs/*.out
docs/*.toc
docs/*.synctex.gz
